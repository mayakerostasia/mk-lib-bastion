# Loki configuration (Minimal Single-node mode)
auth_enabled: false

server:
  http_listen_port: 3100
  log_level: info
  
ingester:
  chunk_idle_period: 3m
  chunk_retain_period: 1m
  max_chunk_age: 1h

limits_config:
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  allow_structured_metadata: false

schema_config:
  configs:
    - from: 2020-10-24
      store: boltdb
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

storage_config:
  boltdb:
    directory: /var/loki/boltdb
  filesystem:
    directory: /var/loki/chunks

table_manager:
  retention_deletes_enabled: false
  retention_period: 0s
