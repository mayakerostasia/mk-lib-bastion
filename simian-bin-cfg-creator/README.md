# simian-bin-cfg-creator

Maya: I don't know if I like the way this works. This seems harder to manage, but it should be making it easier to manage


## Example cfg_creator help
```sh
Usage: simian-bin-cfg-creator.exe <COMMAND>

Commands:
  surreal
  autotask
  jira
  swimlane
  help      Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help
  -V, --version  Print version
```

## Example cfg_creator surreal
```sh
Usage: simian-bin-cfg-creator.exe surreal --uri <URI> --ns <NS> --db <DB> --user <USER> --pass <PASS> --auth-level <AUTH_LEVEL>

Options:
      --uri <URI>
      --ns <NS>
      --db <DB>
      --user <USER>
      --pass <PASS>
      --auth-level <AUTH_LEVEL>
  -h, --help                     Print help
```
